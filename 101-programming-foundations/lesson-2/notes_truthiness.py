'''
 Python evaluates almost all values as true. 
 There are some exceptions
        False
        None
        0
        0j
        0.0
        "" (an empty string)
        [] (an empty list)
        {} (an empty dictionary)
        () (an empty tuple)
        set() (an empty set)
        frozenset() (an empty frozenset)
        range(0) (an empty range)

'''